query GetPowerTargetLimits {
    bosminer {
        metadata {
            autotuning {
                powerTarget {
                    min
                    max
                    default
                    unit
                }
            }
        }
    }
}